/**
 * Common unchecked exception hierarchy used throughout the platform, including exceptions that map
 * to specific HTTP status codes when handled by the REST layer's global exception handler.
 */
package com.stano.exceptions;
