/**
 * Environment bootstrapping support, in particular layering the platform's {@code msp-default.yaml}
 * configuration defaults underneath a consuming application's own configuration.
 */
package com.stano.spring_boot_application.config;
