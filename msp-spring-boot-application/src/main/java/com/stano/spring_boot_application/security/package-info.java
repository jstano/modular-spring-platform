/**
 * Default Spring Security auto-configuration for platform applications: a baseline security filter
 * chain (session, CORS, CSRF, OAuth2 resource server) that applications can override or extend by
 * supplying their own beans or {@code Customizer<HttpSecurity>} beans.
 */
package com.stano.spring_boot_application.security;
