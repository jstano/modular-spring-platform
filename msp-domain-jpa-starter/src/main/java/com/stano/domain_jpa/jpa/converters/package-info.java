/**
 * JPA {@link jakarta.persistence.AttributeConverter} implementations that map value types owned by
 * other platform modules — encrypted text and binary data, hashed passwords, and monetary amounts —
 * onto plain database columns.
 */
package com.stano.domain_jpa.jpa.converters;
