/**
 * Database schema installation and migration support: auto-configuration that provisions a
 * datasource and installs or migrates the application's schema on startup, ahead of JPA's own
 * datasource auto-configuration.
 */
package com.stano.schema;
